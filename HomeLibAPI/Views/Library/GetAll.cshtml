@model IEnumerable<HomeLibAPI.Models.LibraryElementDto>

@{ ViewData["Title"] = "Library Elements"; }

<h2>Index</h2>

<p>
    <!--<a asp-action="Create">Create New</a>-->
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TableOfContents)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TableOfContents)
        </td>
        <td>
            <!--<a asp-action="Update" asp-route-id="@item.Id">Edit</a>-->
            <a asp-action="GetById" asp-route-id="@item.Id">Details</a> |
            <a asp-action="Delete" method="post" asp-route-id="@item.Id">Delete</a>
        </td>
    </tr>}
    </tbody>
</table>
